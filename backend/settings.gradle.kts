plugins {
    // Auto-downloads the JDK matching the toolchain (Java 25) when missing locally
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "itaca-backend"
